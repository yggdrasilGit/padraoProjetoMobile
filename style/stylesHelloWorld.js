import { StyleSheet } from 'react-native';

export const stylesHelloWorld = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#ff0255',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
